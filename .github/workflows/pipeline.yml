name: reactcicd
 
on:
    push:
        branches: [viteDevelopment]
 
jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Deploy NodeJS app
              uses: appleboy/ssh-action@v1.0.0
              with:
                  host: '13.232.55.215'
                  username: 'root'
                  key: ${{ secrets.SSHKEY }}
                  script: sh /var/www/hello-team.sh
